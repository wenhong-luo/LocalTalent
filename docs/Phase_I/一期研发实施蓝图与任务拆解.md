# LocalTalent 第一期研发实施蓝图与任务拆解（最终版）

> 来源：`一期研发实施蓝图与任务拆解.html`。
> 用途：开发前必读 Markdown 版；保留原文内容，移除 HTML/CSS 展示噪音，并补充开发前必读卡与执行索引。

## 开发前必读卡

- **文件定位**：一期里程碑、任务拆解、测试矩阵、部署运维和风险清单文件。
- **实现约束**：用于排期与验收复核；设计权威仍以 Step1–Step4 和 00_MASTER_BOUNDARY 为准。
- **禁止越界点**：禁止把后续阶段、可选假设或建议项提前实现为当前轮功能。
- **下游影响**：约束每轮任务优先级、测试矩阵、CI/CD、Runbook、安全与合规验收。
- **自动化验收关注点**：阶段 DoD、测试矩阵覆盖、安全合规清单、上线前回归与边界扫描。

**统一硬边界提醒**：人才服务区仅展示发布快照；对接接口仅 stub；不得输出原始候选人数据；同意、撤回、数据域、字段级权限必须在服务端收口。

## 执行索引

- 输入与假设
- 第一期目标与范围硬边界
- 里程碑与阶段划分
- 详细任务拆解
- 测试与验收矩阵
- 部署与运维要点
- 安全与合规检查清单与风险清单
- 输出格式与交付物清单

**执行摘要** 本报告将已定稿的 Step1–Step4 设计文档转译为「地方人才服务平台一期」可交付、可排期、可验收的研发实施蓝图与任务拆解。重点收口：一期硬边界（禁止接入 ATS/受控候选池原始主库）、两类数据产品隔离（原始候选人数据 vs 发布快照）、人才服务区仅展示发布快照、RBAC + 数据域 + 字段级权限 + 导出审批 + 审计日志闭环落地。设计权威以 Step1–Step4 为准。fileciteturn1file0 fileciteturn1file1 fileciteturn1file2 fileciteturn1file3

当前日期（报告基准）：2026-04-12（America/Los_Angeles）。里程碑起始日期按要求假定为 2026-04-13。

## 输入与假设

### 已接收输入文件清单

| 输入项 | 文件名 | 状态 | 权威性说明 |
| --- | --- | --- | --- |
| Step1：范围冻结与平台边界 | LocalTalent_step1_修订版_术语统一版.html | **已接收** | 一期范围硬约束来源：禁止接入 ATS/受控候选池原始主库；人才服务区只展示发布快照。fileciteturn1file0 |
| Step2：合规与数据治理上位约束 | LocalTalent_step2_修订版_术语统一版.html | **已接收** | 生命周期矩阵 + 核心边界字段归属矩阵为 DDL/权限/日志的上位约束。fileciteturn1file1 |
| Step3：原型结构、状态原型、对接接口契约 | LocalTalent_step3_修订版_术语统一版.html | **已接收** | 四大页面域 + A/B/C/D/E 接口分组；关键状态原型与对接接口统一契约（签名/幂等/trace/重试）。fileciteturn1file2 |
| Step4：数据库与权限一体化最终定稿 | LocalTalent_step4_术语统一版.html | **已接收** | DDL + 权限模型（RBAC/数据域/字段级权限/审批/审计）与接口×权限×审批×日志矩阵最终口径。fileciteturn1file3 |

### 实施必须假定但在 Step1–Step4 未明确的要素

下列要素若不补齐，会直接影响排期、架构选型与运维成本；在一期启动会前需由 Tech Lead/运维/安全确认（本报告先给出默认假设，便于推进）。

| 要素 | 需要确认的问题 | 默认假设（可替换） |
| --- | --- | --- |
| 部署形态 | 云/自建；K8s/虚机；是否已有网关、WAF、日志平台 | 建议 K8s（或最小 Docker Compose）；接入统一网关；集中日志（ELK/云日志） |
| 认证体系 | 是否已有 SSO（OAuth2/OIDC）；前台/企业/运营是否统一身份源 | 一期可用本地账号 + JWT；若已有 SSO 则优先对接（不改变权限模型） |
| 容量指标 | 门户 QPS、峰值并发、候选人/职位规模、导出文件大小与频次 | 以“可水平扩展”为设计前提；导出采用异步 + 对象存储短期下载 |
| 对象存储与文件策略 | 附件简历/证据/导出文件存储；是否要求加密、版本、生命周期 | MinIO（S3 兼容）+ 分桶分层；短期预签名 URL；生命周期策略（存储期限） |
| 通知通道 | 短信/邮件/站内信；模板与频率；失败重试 | 一期可先站内通知/邮件（可选），不作为核心阻断项 |
| 合规附件 | 同意文本版本管理；撤回入口；权利请求处理流程 | 同意版本入库 + 可追溯；撤回“便捷方式”；保留审计链 |

**术语强制统一**：人才服务区、发布快照、原始候选人数据、同意、撤回、对接接口、数据域、字段级权限。以上在 Step1–Step4 已统一并作为一期研发评审用语。fileciteturn1file0 fileciteturn1file1 fileciteturn1file2 fileciteturn1file3

**技术栈说明（建议而非强制）**：若未指定，推荐 Java/Spring Boot + MySQL/Postgres + Redis + MinIO + React/TypeScript，用于降低招人成本与提升工程化成熟度；但不改变 Step4 的 DDL/权限/审计要求。fileciteturn1file3

## 第一期目标与范围硬边界

### 第一期必须实现的业务功能

一期目标是“地方人才服务平台独立可运行”，实现门户前台、求职者中心、企业后台、运营后台的核心闭环，并满足合规与审计要求。fileciteturn1file0 fileciteturn1file2

| 域 | 必须能力（一期） | 最小验收点（示例） |
| --- | --- | --- |
| 门户前台 | 职位/企业/活动/资讯浏览；人才服务区（发布快照列表/详情） | 人才服务区接口与页面仅展示发布快照字段，不出现原始候选人数据。fileciteturn1file2 |
| 求职者中心 | 账号与简历；投递记录；活动报名；面试签到；同意/撤回与状态展示 | 同意→生成发布快照→人才服务区可见；撤回→触发下线→人才服务区不可见。fileciteturn1file1 |
| 企业后台 | 企业入驻与认证；职位管理（提交审核/上线/下线）；投递池与面试场次管理 | 企业认证待审核不可正式发布职位；职位待审核前台不可见；状态与运营审核一致。fileciteturn1file2 |
| 运营后台 | 企业审核、职位审核；内容/活动管理；导出审批；审计中心；对接任务中心 | 导出候选人明细必须走 export_apply 审批，审批通过才可下载；全链路可审计。fileciteturn1file3 |
| 权限与审计 | RBAC + 数据域 + 字段级权限；审计日志打点；字段访问日志 | 越权（BOLA）与字段暴露（BOPLA）在服务端被阻断并可追溯。fileciteturn1file3 |
| 对接接口（仅 Stub） | 对接接口鉴权 open_client；签名/幂等/trace/重试框架；open_api_log；同步任务表 | 接口契约通过；但不打穿 ATS/受控候选池原始主库。fileciteturn1file0 fileciteturn1file2 |

### 第一期禁止项与数据边界

**硬禁止项** 一期禁止直接接入 ATS 原始候选人数据；一期禁止直接接入受控候选池原始主库；一期仅允许预留对接接口位与结构兼容位，不得提前落地两系统业务能力。人才服务区仅展示候选人发布快照，不展示原始简历、联系方式、聊天记录、附件、证据材料。fileciteturn1file0

**两类数据产品必须隔离** 原始候选人数据（用于业务处理与留痕）与发布快照（用于人才服务区公开展示）是两类不同数据产品：发布快照必须经过同意、裁剪/脱敏与可见范围控制；人才服务区仅读发布快照。fileciteturn1file1 fileciteturn1file3

### 一期 DoD（Definition of Done）总则

- **范围 DoD**：无任何“直连 ATS/受控候选池原始主库”的代码路径、配置项或数据同步任务；对接接口仅 stub。fileciteturn1file0
- **数据边界 DoD**：人才服务区任一接口与页面均不返回原始候选人数据字段；发布快照字段清单可审计且可回归测试。fileciteturn1file3
- **权限 DoD**：RBAC + 数据域 + 字段级权限在服务端强制执行；任何对象 ID 查询均做对象级授权校验（对应 OWASP API1:2023 BOLA 风险）。citeturn4search16
- **审计 DoD**：关键动作写 audit_log；敏感字段读取写 field_access_log；日志不记录敏感明文（对应 OWASP A09 & Logging Cheat Sheet）。citeturn1search5turn1search17
- **合规 DoD**：提供便捷撤回同意方式并生效；遵循合法、正当、必要、最小范围收集等原则（PIPL）。citeturn3view0

## 里程碑与阶段划分

### 一期内部阶段划分与交付物

| 阶段 | 目标 | 交付物清单 | 关键风险与缓解 |
| --- | --- | --- | --- |
| P0 基座 | 可开发、可部署、可迁移 | 仓库骨架（后端/前端/脚本）；DB 迁移框架；字典与最小权限种子数据；统一错误码/trace_id/日志规范；对象存储接入骨架。fileciteturn1file3 | 风险：迁移脚本不可控、环境差异。<br>缓解：迁移版本化 + staging 演练；发布流程“先迁移后发布”。 |
| P1 核心闭环 | 地方平台独立可运行 | 权限（RBAC/数据域/字段级权限）最小可用；候选人同意/撤回/发布快照；企业认证/职位审核/上下线；门户人才服务区展示；审计日志最小闭环。fileciteturn1file2 fileciteturn1file3 | 风险：访问控制失效（OWASP A01）与对象级越权（API1 BOLA）。<br>缓解：服务端强制鉴权与数据域注入；IDOR 测试用例阻断。citeturn4search2turn4search16 |
| P2 运营合规完善 | 可审核、可导出、可追溯 | 运营审核工作台；导出审批 export_apply + 一次性下载；字段访问审计完善；生命周期关键节点（下线/冻结/留痕归档）任务化。fileciteturn1file1 fileciteturn1file3 | 风险：导出绕过与敏感字段泄露（API3 BOPLA）。<br>缓解：导出强制审批；字段级权限对导出同样生效；字段访问写日志。citeturn4search1turn2search2 |
| P3 上线准备 | 可验收、可回滚、可观测 | 全量测试与验收报告；安全与合规检查清单签署；预发演练与回滚演练；监控告警与日志采集；上线清单。fileciteturn1file3 | 风险：日志与监控不足导致不可定位（OWASP A09）。<br>缓解：关键指标+告警门槛；trace_id 贯通；审计链抽查。citeturn1search5turn1search17 |

### 时间线图

起始日期按要求使用 2026-04-13；具体工期随人力配置与复用基础设施程度调整。

```text
gantt
  dateFormat  YYYY-MM-DD
  title LocalTalent 一期实施时间线（建议）
  section P0 基座
  仓库骨架/规范/CI 最小闭环            :p0a, 2026-04-13, 5d
  DB 迁移框架 + 字典/权限种子数据       :p0b, after p0a, 5d
  section P1 核心闭环
  权限引擎（RBAC+数据域+字段级）        :p1a, after p0b, 10d
  候选人链路（同意/撤回/发布快照/展示） :p1b, after p1a, 10d
  企业&职位（认证/审核/上下线）         :p1c, after p1a, 10d
  section P2 运营合规完善
  运营审核台 + 审计中心                 :p2a, after p1c, 10d
  导出审批与一次性下载 + 生命周期任务    :p2b, after p1b, 10d
  section P3 上线准备
  全量测试/安全合规评审/预发演练         :p3a, after p2b, 7d
  上线窗口 + 回滚演练                    :p3b, after p3a, 3d
```

### 低/中/高人员配置与工期估算

| 配置 | 人员构成（示例） | 预计工期 | 适用条件 |
| --- | --- | --- | --- |
| 低配 | 后端×1、前端×1、测试×0.5、DevOps×0.2、安全/合规×0.2、PM×0.5 | 10–12 周 | 基础设施复用度高；对接接口仅 stub；验收节奏宽松 |
| 中配（推荐） | 后端×2、前端×2、测试×1、DevOps×0.5、安全/合规×0.3、PM×0.5 | 8–10 周 | 需要完整权限/审计/导出审批闭环；上线演练严格 |
| 高配 | 后端×3、前端×3、测试×2、DevOps×1、安全/合规×0.5、PM×0.5 | 6–8 周 | 并行推进多模块；更高测试覆盖与性能/安全验证 |

## 详细任务拆解

### 后端任务拆解

后端以 Step4 的逻辑分库与权限模型为底座，优先实现“访问控制与审计闭环”，再实现业务链路，最后实现对接接口 stub。fileciteturn1file3

| 模块 | 子任务（接口/服务/策略） | DB 变更与表 | 验收要点（DoD） |
| --- | --- | --- | --- |
| 统一基础设施 | 统一响应结构；统一错误码；trace_id 贯通；统一鉴权中间件；统一审计切面；幂等键处理器（写接口/回调）。fileciteturn1file2 | open_api_log、audit_log、field_access_log（含 trace_id）fileciteturn1file3 | 所有接口返回包含 trace_id；错误码稳定；日志不含敏感明文 |
| 权限域 | RBAC（角色/菜单/接口权限）；数据域注入（本人/本企业/本岗位等）；字段级权限引擎（ALLOW/MASK/DENY）；导出审批策略；权限缓存。fileciteturn1file3 | sys_role、sys_menu、sys_role_menu、sys_user_role、sys_role_data_scope、sys_role_field_policy fileciteturn1file3 | 服务端强制数据域过滤；字段级权限在出参层统一裁剪；无权限返回空态/403 |
| 候选人域 | 账号/简历 CRUD；同意（记录+证据）；撤回（联动下线）；发布快照生成/更新/下线；人才服务区查询仅基于快照。fileciteturn1file1 | candidate_user、candidate_resume、candidate_consent、candidate_consent_evidence、candidate_publish_snapshot、candidate_control_profile fileciteturn1file3 | 同意→生成发布快照；撤回→publishable_flag 变更并下线快照；快照字段最小必要与脱敏 |
| 企业与职位域 | 企业入驻与认证申请；认证审核；职位创建/提交审核/上线/下线；状态机与页面原型一致。fileciteturn1file2 | company、company_user、company_auth_apply、job_post（含 job_status 字典）fileciteturn1file3 | 企业认证待审核不可发布职位；职位待审核门户不可见；状态变更写审计日志 |
| 投递与面试域 | 投递（job_application）；投递状态流转；面试场次与签到；与生命周期矩阵一致。fileciteturn1file1 | job_application、interview_session、interview_signin fileciteturn1file3 | 投递数据域隔离（企业仅可见本企业投递）；关键状态变更审计留痕 |
| 运营与导出域 | 审核队列（企业/职位）；导出申请/审批/生成下载；审计查询；对接任务中心。fileciteturn1file3 | export_apply、audit_log、integration_sync_task（展示用）fileciteturn1file3 | 导出必须走审批；下载采用短期链接；导出字段级权限仍生效 |
| 对接接口域（Stub） | open_client 鉴权；签名校验；重放防护；幂等；回调重试框架与失败告警；不接入 ATS/受控候选池主库。fileciteturn1file0 fileciteturn1file2 | open_client、open_api_log、integration_mapping、integration_sync_task fileciteturn1file3 | 契约测试通过（签名/幂等/trace/重试）；无任何主库直连配置 |

### DDL 实施顺序与迁移脚本要点

按 Step4 的分层原则实施：先字典与权限附加表，再核心业务表，再对接/审计/证据表，降低依赖与联调成本。fileciteturn1file3

| 批次 | 目标 | 主要表 | 要点 |
| --- | --- | --- | --- |
| DDL-0 | 字典与枚举 | sys_dict_type、sys_dict_item | 状态类字段用小整数+字典解释，避免魔法字符串。fileciteturn1file3 |
| DDL-1 | 权限附加表 | sys_role、sys_menu、sys_role_menu、sys_user_role、sys_role_data_scope、sys_role_field_policy | 权限先行，便于后续接口联调与验收。fileciteturn1file3 |
| DDL-2 | 核心业务主表 | company、company_user、candidate_user、candidate_resume、candidate_control_profile、job_post | **candidate_control_profile 必须优先落库** 以承载边界字段当前生效状态主归属。fileciteturn1file3 |
| DDL-3 | 流程与快照 | job_application、interview_session、interview_signin、candidate_consent、candidate_publish_snapshot、activity_event、activity_registration、cms_content | 发布快照与原始候选人数据表严格分离，防止误用。fileciteturn1file1 |
| DDL-4 | 对接/审计/证据 | open_client、open_api_log、integration_mapping、integration_sync_task、audit_log、field_access_log、export_apply、candidate_consent_evidence | trace_id 贯通 open_api_log 与 audit_log，便于定位与举证。fileciteturn1file3 |

- **迁移脚本策略**：版本化迁移“只前进不回写”，生产环境优先 roll-forward；staging 必须演练“迁移+回滚/或向前修复”。
- **初始化数据**：字典、默认角色、菜单与最小 role-menu 映射使用单独 seed；seed 可重复执行不产生重复数据（依赖唯一键）。

### 接口实现优先级与简洁工程规范

对接接口统一契约在 Step3 已明确（签名/幂等/错误码/trace/版本/重试）。建议一期把这些要素提升为全站 API 基础规范，对接接口为强制项。fileciteturn1file2

| 规范项 | 一期统一规则 | 参考依据 |
| --- | --- | --- |
| trace 规范 | 请求头必须包含 X-Trace-Id（无则生成并回传）；对接接口可兼容 W3C traceparent 以支持分布式追踪互操作 | W3C 已发布 Trace Context 推荐标准（中文版新闻稿）。citeturn0search27 |
| 幂等规范 | 写接口/回调必须携带 X-Idempotency-Key；服务端以（接口+幂等键）保证重复请求返回一致结果 | Step3 对接接口统一契约。fileciteturn1file2 |
| 重试规范 | 对接回调失败进入 integration_sync_task；指数退避或 1/5/15/60min 阶梯退避；到达上限转失败并告警 | Step3 对接接口统一契约。fileciteturn1file2 |
| 错误码规范 | 统一响应：{code,message,trace_id,data}；对接接口错误码至少分：认证/签名/参数/业务状态/权限/重试/系统 | Step3 对接接口统一契约；可选采用 RFC 7807 Problem Details。fileciteturn1file2 citeturn2search1 |
| 签名与防重放 | client_code + timestamp + nonce + body_hash 签名；校验时间窗与 nonce 去重；open_client 绑定 scope | Step3 契约 + Step4 open_client/open_api_log。fileciteturn1file2 fileciteturn1file3 |

### 前端任务拆解

前端按 Step3 四大页面域落地，并强制覆盖状态原型：企业认证（待审核/驳回/通过）、职位（待审核/在线/下线）、候选人发布控制（已同意/已撤回/不可发布）、无权限查看、对接任务（同步失败/重试中）。fileciteturn1file2

| 页面域 | 页面/路由清单（一期） | 关键组件与验收点 |
| --- | --- | --- |
| 门户前台 | 首页、职位列表/详情、企业列表/详情、活动列表/详情、资讯列表/详情、人才服务区（发布快照列表/详情） | 搜索筛选/分页；人才服务区字段展示白名单；异常与空态统一 |
| 求职者中心 | 账号与安全、简历管理、投递记录、活动报名记录、面试签到、同意与撤回（状态页） | 同意/撤回状态机；上传组件（简历附件）；撤回后提示“已撤回并下线” |
| 企业后台 | 企业认证提交/补充/驳回重提、职位管理（草稿/提交审核/上下线）、投递管理、面试场次管理 | 状态条组件；待审核限制操作；数据域提示（仅本企业） |
| 运营后台 | 企业审核、职位审核、内容/活动管理、导出审批、审计中心、对接任务中心 | 审批流组件；导出审批通过后才生成下载入口；审计查询可按 trace_id 定位 |

### 状态机与状态覆盖矩阵

| 场景 | 状态 | 页面表现（一期必须覆盖） | 后端依赖/验收点 |
| --- | --- | --- | --- |
| 企业认证 | 待审核/驳回/通过 | 显示状态条与原因；待审核与驳回限制功能入口 | company_auth_status 字典一致；状态变更写 audit_log。fileciteturn1file3 |
| 职位 | 待审核/在线/下线 | 前台可见性与后台状态一致；下线后前台不可见 | job_status 字典一致；审核通过才能 online。fileciteturn1file3 |
| 候选人发布控制 | 已同意/已撤回/不可发布 | 同意后提示“可生成发布快照”；撤回后提示“已撤回且已下线” | candidate_control_profile 主控；撤回触发快照下线。fileciteturn1file3 |
| 无权限查看 | 403/字段被 DENY | 统一空态页；不得“接口返回但前端隐藏” | 服务端字段级权限裁剪（API3 风险控制）。citeturn4search1 |
| 对接任务 | 同步失败/重试中 | 显示错误信息与 trace_id；展示重试次数 | integration_sync_task & open_api_log 可追踪。fileciteturn1file3 |

### 联调批次与验收标准

| 批次 | 联调范围 | 验收标准（摘取关键项） |
| --- | --- | --- |
| INT-0 | 登录/Token、字典、门户公开接口 | 门户可浏览职位与人才服务区；全链路 trace_id 可定位请求 |
| INT-1 | 求职者：简历、同意、发布快照生成、撤回 | 同意→人才服务区可见；撤回→不可见；审计日志可查到关键事件。fileciteturn1file1 |
| INT-2 | 企业：认证、职位管理与运营审核联动 | 认证待审核不可发布；职位待审核前台不可见；审核通过才上线。fileciteturn1file2 |
| INT-3 | 运营：导出审批、审计中心、对接任务中心 | 导出必须先申请再审批；审批通过才下载；下载为短期链接且留痕。fileciteturn1file3 |
| INT-4 | 对接接口（stub）契约联调 | 签名/幂等/重试/错误码规范通过；open_api_log 记录完整。fileciteturn1file2 |

### 数据库关键表清单与 candidate 边界字段主归属

一期关键表应与 Step4 保持一致，且 candidate 核心边界字段必须集中在 candidate_control_profile 作为“当前生效状态主归属”。fileciteturn1file3

| 类别 | 关键表（一期最小集合） | 用途说明 |
| --- | --- | --- |
| 候选人（原始候选人数据） | candidate_user、candidate_resume | 原始候选人数据仅用于业务处理与留痕；不得进入人才服务区展示链路。fileciteturn1file3 |
| 候选人（同意/快照） | **candidate_control_profile**、candidate_consent、candidate_publish_snapshot、candidate_consent_evidence | control_profile：source_type/legal_basis/consent_status/publishable_flag/visibility_scope/consent_version 当前值主控；consent 存历史；snapshot 存可展示副本。fileciteturn1file3 |
| 企业与职位 | company、company_user、company_auth_apply、job_post | 企业认证与职位审核/上下线的状态机来源；字典化状态字段。fileciteturn1file3 |
| 投递与面试 | job_application、interview_session、interview_signin | 支持投递与签到链路，与生命周期矩阵一致。fileciteturn1file1 |
| 权限表 | sys_role、sys_menu、sys_role_menu、sys_user_role、sys_role_data_scope、sys_role_field_policy | RBAC + 数据域 + 字段级权限策略落库。fileciteturn1file3 |
| 对接接口（stub） | open_client、open_api_log、integration_mapping、integration_sync_task | 对接鉴权、调用日志与重试任务；不连接 ATS/候选池主库。fileciteturn1file0 |
| 审计与审批 | audit_log、field_access_log、export_apply | 关键动作审计；敏感字段访问审计；导出审批闭环。fileciteturn1file3 |

## 测试与验收矩阵

一期验收必须覆盖功能正确性、接口契约、权限（对象级/数据域/字段级）、合规（同意/撤回/保留）、性能、安全与审计链。访问控制失效是 OWASP Top 10 的首要风险类别之一，应纳入阻断式验收。citeturn4search2

| 测试类型 | 必须覆盖的关键用例（示例） | 通过口径 |
| --- | --- | --- |
| 单元测试 | 字段级权限引擎（ALLOW/MASK/DENY）；数据域过滤器；同意/撤回状态更新与联动下线 | 关键策略分支全覆盖；边界条件（空值/异常）覆盖 |
| 集成测试（DB） | candidate_control_profile 与 candidate_consent 历史一致性；发布快照生成/下线；导出审批状态流转 | 迁移脚本可在空库与升级库成功执行；唯一索引/外键约束生效 |
| 接口测试（契约） | 对接接口签名校验；幂等键重复请求；错误码与 trace_id；回调失败入重试队列 | 契约测试全绿；日志可按 trace_id 回溯 |
| 权限测试 | BOLA：尝试用他人 candidate_id/job_id 获取详情；<br>数据域：企业 A 不能查看企业 B 投递；<br>字段级：无权限角色获取手机号/邮箱/简历正文应 DENY 或 MASK | 越权请求被阻断（403/404）；字段不出现在响应或以脱敏形式出现；访问写 field_access_log |
| 合规测试 | 同意前不可进入人才服务区；撤回后必须下线；删除/冻结与留痕归档（按生命周期矩阵） | 生命周期矩阵逐条验收通过；撤回方式“便捷”且生效（PIPL 第十五条）。citeturn3view0 |
| 性能测试 | 门户职位列表与人才服务区快照列表分页；导出异步生成对主链路无阻塞；审计日志写入吞吐 | 满足约定 P95/P99；导出不阻塞核心交易路径；重试不引发雪崩 |
| 安全测试 | 访问控制（OWASP A01）、日志与监控（OWASP A09）、API1 BOLA、API3 BOPLA、注入与配置基线 | Top 风险条目映射检查单通过；无敏感信息落日志（OWASP Logging Cheat Sheet）citeturn1search5turn1search17turn4search16turn4search1 |

## 部署与运维要点

### 环境与发布流程

- **环境**：dev / staging / prod 至少三套；staging 具备与 prod 一致的 DB 迁移与对象存储策略。
- **发布顺序**：先执行 DB 迁移（DDL-0..DDL-4）→ 再滚动发布应用 → 冒烟测试（关键链路）→ 指标观察。
- **健康检查**：若采用 K8s，应配置就绪/存活/启动探针，避免未就绪流量进入并支持自愈。citeturn1search3
- **回滚策略**：业务代码可回滚；DB 结构优先 roll-forward（通过新增迁移修正），生产避免直接回滚结构。

### 备份、监控告警与日志采集

- **备份**：DB 全量 + 增量（binlog/WAL）；定期恢复演练；审计库与证据元数据同等级保护。
- **监控指标**：接口 P95/P99、5xx、导出审批堆积、重试队列长度、DB 连接池、对象存储 4xx/5xx。
- **日志采集**：统一采集应用日志 + 审计日志；按 trace_id 关联；禁止记录敏感明文（对应 OWASP A09 与日志最佳实践）。citeturn1search5turn1search17

### 对象存储分层与短期下载策略

- **分桶建议**：resume（简历附件）、snapshot（发布快照资源）、evidence（同意证据）、export（导出文件）。
- **短期下载**：导出文件与证据下载使用短期预签名 URL；导出必须先经 export_apply 审批通过。fileciteturn1file3
- **生命周期**：按保存期限配置自动过期/归档；与合规保留策略一致。fileciteturn1file1

## 安全与合规检查清单与风险清单

### 安全与合规检查清单

| 检查项 | 一期要求 | 依据 |
| --- | --- | --- |
| 合法、正当、必要、最小范围 | 收集与处理必须目的明确、与目的直接相关；禁止过度收集；公开透明告知 | PIPL 总则与处理规则（含最小必要）。citeturn3view0 |
| 同意与撤回 | 同意前不得进入发布链路；提供便捷撤回方式；撤回后触发下线与停止同步 | PIPL 第十五条要求提供便捷撤回方式。citeturn3view0 |
| 人才服务区数据边界 | 人才服务区只展示发布快照；原始候选人数据不得返回/渲染 | Step1/Step3/Step4 边界硬约束。fileciteturn1file0turn1file2turn1file3 |
| 访问控制与越权防护 | 对象级授权 + 数据域过滤 + 默认拒绝；所有 ID 查询均二次校验 | OWASP A01（访问控制失效）、OWASP API1:2023（BOLA）。citeturn4search2turn4search16 |
| 字段级保护 | 手机号/邮箱/简历正文/证据材料默认 DENY 或 MASK；服务端统一裁剪；字段访问留痕 | OWASP API3:2023（对象属性级授权失效）强调字段级授权。citeturn4search1 |
| 日志脱敏与监控 | 审计日志/安全日志完整；不写敏感明文；告警与响应机制明确 | OWASP A09 & Logging Cheat Sheet。citeturn1search5turn1search17 |
| 个人信息安全规范对齐 | 对齐国家推荐标准 GB/T 35273-2020 的基本原则与要求（作为治理与评审参考） | 国家标准全文公开页面（权威元信息）。citeturn1search2 |

### 接口与权限审批日志映射表

下表为一期研发、测试与安全评审的“统一检查点”，直接继承 Step4 的接口×权限×审批×日志矩阵。fileciteturn1file3

| 接口分组 | 允许角色 | 数据域控制 | 审批 | 日志要求 |
| --- | --- | --- | --- | --- |
| A 门户公开接口 | visitor / candidate / company_user / operator | 无（仅公开展示层） | 否 | 不写 field_access_log；确保不返回敏感字段 |
| B 求职者中心接口 | candidate | 本人边界 | 同意/撤回不审批 | 同意/撤回写 audit_log；高敏字段访问写 field_access_log |
| C 企业后台接口 | company_admin / recruiter / interviewer | 本企业/本岗位/本场次 | 导出是 | 状态变更写 audit_log；读取高敏字段写 field_access_log |
| D 运营后台接口 | operator / auditor | 聚合优先；明细授权 | 审核类按流程；导出审批 | 审核/发布/下线写 audit_log；查阅高敏字段写 field_access_log |
| E 对接接口（stub） | open_client | 按 api_scope 与 biz_type | 否 | open_api_log 必写；失败入重试队列；通常不写 field_access_log |

### 关键风险与缓解措施清单

| 风险 | 触发点 | 缓解措施（一期必须落地） | 验证方式 |
| --- | --- | --- | --- |
| 访问控制失效（A01）导致越权 | 接口仅做登录校验，未做对象级授权与数据域过滤 | 服务端强制数据域注入；所有按 ID 查询二次归属校验；默认拒绝 | IDOR 用例矩阵 + 渗透脚本；阻断式验收citeturn4search2turn4search16 |
| 字段暴露（API3）导致敏感泄露 | 响应体包含手机号/邮箱/简历正文/证据 URL 等 | 字段级权限引擎统一裁剪/脱敏；人才服务区字段白名单 | 接口出参快照测试 + field_access_log 断言citeturn4search1turn2search2 |
| 同意/撤回链路不一致 | 同意记录写入但 publishable_flag 未更新；撤回未触发下线 | 以 candidate_control_profile 为当前生效状态主控；撤回触发快照下线与审计 | 生命周期矩阵回归（同意→发布→撤回→下线）fileciteturn1file1turn1file3 |
| 导出审批绕过 | 运营或企业绕过 export_apply 直接导出 | 导出能力仅经审批流开放；下载短期链接；导出字段级权限生效 | 权限用例 + 审批流用例；下载事件审计fileciteturn1file3 |
| 日志与监控不足（A09） | 无告警、无 trace_id 关联、日志记录敏感明文 | 统一日志规范；trace_id 贯通；敏感信息脱敏；告警阈值与升级流程 | 演练：故障注入 + 定位；日志抽检citeturn1search5turn1search17 |
| 对接接口重放/伪造 | 签名/时间窗/nonce 未校验或密钥泄露 | 签名 + 时间窗 + nonce 去重；密钥分环境管理与轮换；open_client scope 最小化 | 契约安全测试：重放/伪造/过期请求必须失败fileciteturn1file2turn1file3 |

### 输出格式与交付物清单

以下为一期必须交付的文档/工件清单（用于研发、测试、安全、法务联合评审与验收归档）。

| 交付物 | 内容要点 | 产出阶段 |
| --- | --- | --- |
| API 规格（OpenAPI） | 站内 API + 对接接口 stub；错误码与 trace_id；权限注解（数据域/字段级） | P0–P2 |
| DDL 与迁移脚本 | DDL-0..DDL-4；seed 数据（字典、默认角色/菜单）；可重复执行策略 | P0–P1 |
| 权限配置清单 | 角色/菜单/数据域/字段策略默认配置；高敏字段列表与脱敏规则 | P1 |
| 测试用例矩阵与报告 | 功能/接口/权限/合规/性能/安全；阻断式验收用例（IDOR/字段泄露/导出绕过） | P2–P3 |
| 部署手册与回滚手册 | 环境变量；发布流程；健康检查；回滚策略（代码回滚 + DB roll-forward） | P3 |
| 运维 Runbook | 监控指标与告警阈值；日志查询（按 trace_id）；故障处理 SOP | P3 |
| 审计材料包 | 审计字段说明；audit_log/field_access_log 字段解释；导出审批链路证据；同意/撤回留痕说明 | P2–P3 |
| 合规说明材料 | 同意文本版本管理；撤回入口说明；保存期限策略；与 PIPL 原则对齐说明 | P2–P3 |

**合规依据提示（用于评审会引用）** PIPL 明确要求个人信息处理遵循合法、正当、必要与最小范围收集原则，并要求提供便捷撤回同意方式。citeturn3view0

**安全依据提示（用于评审会引用）** OWASP Top 10 将“访问控制失效”列为首要风险类别；OWASP API Security Top 10 将“对象级授权失效（BOLA）”列为首要 API 风险，“对象属性级授权失效（BOPLA）”强调字段级授权缺失会导致信息暴露或被未授权操控。citeturn4search2turn4search16turn4search1turn2search2
